<%= form_for @changeset, Routes.topic_path(@conn, :update, @topic), fn f -> %>
  <div class="form-group">
		<%= text_input f, :title, placeholder: @topic.title, class: "form-control" %>
		<%= error_tag f, :title %>
  </div>
	<%= submit "Edit Title", class: "btn btn-primary" %>
<% end %>